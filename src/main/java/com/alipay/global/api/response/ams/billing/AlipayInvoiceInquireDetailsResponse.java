/*
 * v1_billing_invoice_inquireDetails
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.alipay.global.api.response.ams.billing;

import com.alipay.global.api.model.ams.*;
import com.alipay.global.api.model.ams.Amount;
import com.alipay.global.api.model.ams.BillingDiscount;
import com.alipay.global.api.model.ams.InvoiceItem;
import com.alipay.global.api.model.ams.InvoiceNote;
import com.alipay.global.api.model.ams.InvoicePayment;
import com.alipay.global.api.model.ams.InvoiceShipping;
import com.alipay.global.api.model.ams.PaymentMethod;
import com.alipay.global.api.response.AlipayResponse;
import java.util.List;
import lombok.*;

/** AlipayInvoiceInquireDetailsResponse */
@EqualsAndHashCode(callSuper = true)
@Data
public class AlipayInvoiceInquireDetailsResponse extends AlipayResponse {

  /**
   * System-generated invoice ID. Cannot be null. Returned only when result.resultCode is SUCCESS.
   */
  private String invoiceId;

  /**
   * Merchant-supplied idempotency key (echo-back from creation). Cannot be null. Returned only when
   * result.resultCode is SUCCESS.
   */
  private String invoiceRequestId;

  /**
   * Associated subscription ID. Omitted for standalone manual invoices (invoices not linked to a
   * subscription). Returned only when result.resultCode is SUCCESS.
   */
  private String subscriptionId;

  /**
   * Customer ID this invoice belongs to. Cannot be null. Returned only when result.resultCode is
   * SUCCESS.
   */
  private String customerId;

  /**
   * Human-readable sequential invoice number, unique per merchant (e.g., &#x60;INV-0001&#x60;).
   * Generated by &#x60;InvoiceNumberSequenceService&#x60; at creation time. Suitable for display on
   * PDFs and customer-facing pages. Cannot be null. Returned only when result.resultCode is
   * SUCCESS.
   */
  private String invoiceNumber;

  /**
   * Customer&#39;s first name. Populated from the customer record at invoice creation time. PII -
   * see the six-element PII disclosure below the table. Omitted when not set on the customer
   * record. Returned only when result.resultCode is SUCCESS.
   */
  private String customerFirstName;

  /**
   * Customer&#39;s last name. Populated from the customer record at invoice creation time. PII -
   * see the six-element PII disclosure below the table. Omitted when not set on the customer
   * record. Returned only when result.resultCode is SUCCESS.
   */
  private String customerLastName;

  /**
   * Customer&#39;s email address. Populated from the customer record at invoice creation time. PII
   * - see the six-element PII disclosure below the table. Omitted when not set on the customer
   * record. Returned only when result.resultCode is SUCCESS.
   */
  private String customerEmail;

  /**
   * Invoice creation reason: &#x60;MANUAL&#x60;, &#x60;SUBSCRIPTION_CREATION&#x60;,
   * &#x60;SUBSCRIPTION_RECURRENCE&#x60;, &#x60;SUBSCRIPTION_UPDATE&#x60;. Cannot be null. Returned
   * only when result.resultCode is SUCCESS.
   */
  private String reason;

  /**
   * Subscription phase number. Identifies which billing phase generated this invoice. Value range:
   * 1-unlimited. Returned only for subscription-generated invoices (&#x60;reason&#x60; !&#x3D;
   * &#x60;MANUAL&#x60;); omitted for manual invoices. Returned only when result.resultCode is
   * SUCCESS.
   */
  private String phaseNo;

  /**
   * Current invoice status: &#x60;DRAFT&#x60;, &#x60;OPEN&#x60;, &#x60;PAID&#x60;,
   * &#x60;UNCOLLECTIBLE&#x60;, &#x60;VOID&#x60;. Cannot be null. Returned only when
   * result.resultCode is SUCCESS.
   */
  private String status;

  /**
   * Three-letter ISO currency code in uppercase (e.g., &#x60;\&quot;USD\&quot;&#x60;). The invoice
   * currency. All &#x60;Amount.currency&#x60; sub-fields within this response echo the same
   * uppercase code. Cannot be null. Returned only when result.resultCode is SUCCESS.
   */
  private String currency;

  private Amount subtotal;

  private Amount discountAmount;

  private Amount totalAmount;

  private Amount paidAmount;

  private Amount remainAmount;

  private Amount taxAmount;

  /**
   * Payment collection method. Allowed values: &#x60;CHARGE_AUTOMATICALLY&#x60; - automatically
   * charge the customer&#39;s default payment method when the invoice is finalized;
   * &#x60;SEND_INVOICE&#x60; - send the invoice to the customer for manual payment via email.
   * Cannot be null. Returned only when result.resultCode is SUCCESS.
   */
  private String collectionMethod;

  private PaymentMethod paymentMethod;

  private InvoiceShipping shipping;

  /**
   * URL to the invoice hosted page where the merchant/customer can view, download, or pay the
   * invoice. The URL carries an opaque signed view token - it does not expose the raw
   * &#x60;invoiceId&#x60; or &#x60;merchantId&#x60;. Links expire per the view-link validity period
   * (see the &#x60;createViewLink&#x60; API) and are invalidated when the invoice is voided.
   * Returned when: (1) invoice is OPEN (auto-generated during finalization), or (2) invoice is
   * DRAFT and a view link was created via &#x60;createViewLink&#x60; API. Omitted if neither
   * condition is met. Returned only when result.resultCode is SUCCESS.
   */
  private String hostedInvoiceUrl;

  /**
   * Billing period start timestamp (ISO 8601, e.g., &#x60;2026-03-01T00:00:00+00:00&#x60;). Cannot
   * be null. Returned only when result.resultCode is SUCCESS.
   */
  private String periodStart;

  /**
   * Billing period end timestamp (ISO 8601). Cannot be null. Returned only when result.resultCode
   * is SUCCESS.
   */
  private String periodEnd;

  /**
   * Payment due date, format: &#x60;yyyy-MM-dd&#x60; (date-only, no time component). Cannot be
   * null. Returned only when result.resultCode is SUCCESS.
   */
  private String dueDate;

  /**
   * Timestamp when the invoice was fully paid (ISO 8601). Omitted if unpaid. Returned only when
   * result.resultCode is SUCCESS.
   */
  private String paidTime;

  /**
   * Invoice-level description text displayed on the invoice. Note: line items carry their own
   * &#x60;description&#x60; field (max 256) scoped to the individual line item. Omitted when not
   * set. Returned only when result.resultCode is SUCCESS.
   */
  private String description;

  /**
   * Array of line items that make up this invoice. May be empty for invoices with only manual
   * amounts. Cannot be null. Returned only when result.resultCode is SUCCESS.
   */
  private List<InvoiceItem> items;

  /**
   * Payment attempts for this invoice. Queried by merchantId and invoiceId. Omitted if no payment
   * attempts exist. Returned only when result.resultCode is SUCCESS.
   */
  private List<InvoicePayment> payments;

  /**
   * Array of notes attached to the invoice, sorted by time descending (most recent first). Each
   * note captures contextual information about an action performed on the invoice (e.g., void,
   * finalize, paid). Stored in the invoice metadata. Omitted if no notes exist for the invoice.
   * Returned only when result.resultCode is SUCCESS.
   */
  private List<InvoiceNote> invoiceNotes;

  /**
   * ISO 8601 timestamp of invoice creation (e.g., &#x60;2026-03-01T00:59:55+00:00&#x60;). Cannot be
   * null. Returned only when result.resultCode is SUCCESS.
   */
  private String gmtCreate;

  /**
   * ISO 8601 timestamp of last invoice update. Cannot be null. Returned only when result.resultCode
   * is SUCCESS.
   */
  private String gmtUpdate;

  /**
   * Invoice-level discount items applied to this invoice. Each element carries either a
   * &#x60;couponId&#x60; or &#x60;promotionCodeId&#x60;. Parsed from the invoice
   * &#x60;discountItemIds&#x60; JSON column at query time. The computed total is reflected in
   * &#x60;discountAmount&#x60;. Omitted if no discounts are applied. See DiscountItem Object below
   * for field details. Returned only when result.resultCode is SUCCESS.
   */
  private List<BillingDiscount> discounts;

  private Amount postPaymentCreditNotesAmount;

  private Amount prePaymentCreditNotesAmount;

  private Amount paymentDeducted;
}
